import { Theme } from './types';
import { DefaultColors, MaterializeColors } from './colors';

export const DefaultTheme = {
  name: 'default',
  colors: DefaultColors
} as Theme;

export const MaterializeTheme = {
  name: 'materialize',
  colors: MaterializeColors
} as Theme;
